type Query {
    helloWorld: String!
}

type Mutation {
    createGame (config: GameConfigInput!): Game!
}

type Game {
    _id: ID!
    gameType: String!
}

input GameConfigInput {
    gameType: String!
}