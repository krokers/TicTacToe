type Query {
    helloWorld: String!
}

type Mutation {
    createGame (config: GameConfigInput!): Game!
}

type Subscription {
    playerReady: String!
#    playerReady(value: String!): Game!
}

type Game {
    _id: ID!
    gameType: String!
}

input GameConfigInput {
    gameType: String!
}